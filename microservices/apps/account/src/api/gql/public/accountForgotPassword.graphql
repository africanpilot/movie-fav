mutation AccountForgotPassword($accountLogin: String!) {
  accountForgotPassword(accountLogin: $accountLogin) {
        response {
            code
            success
            message
            version
        }
        pageInfo {
            page_info_count
        }
    }
}